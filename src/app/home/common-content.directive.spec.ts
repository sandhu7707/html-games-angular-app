import { CommonContentDirective } from './common-content.directive';

describe('CommonContentDirective', () => {
  it('should create an instance', () => {
    const directive = new CommonContentDirective();
    expect(directive).toBeTruthy();
  });
});
